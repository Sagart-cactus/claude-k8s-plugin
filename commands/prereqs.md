---
description: Check and install prerequisites (kind, kubectl, kustomize, tilt, go, kubebuilder).
---
Run `${CLAUDE_PLUGIN_ROOT}/scripts/prereqs.sh`.

This auto-detects your OS and installs any missing tools needed for Kubernetes operator development.

Required tools: kind, kubectl, kustomize, tilt, go, kubebuilder.
